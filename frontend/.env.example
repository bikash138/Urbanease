# Backend API URL - must match where your backend runs
# Backend should set FRONTEND_URL to http://localhost:3000 (or this app's URL) for CORS
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_IMAGEKIT_BASE_URL=https://ik.imagekit.io/*****