version: "3.8"
services:
  urbanease-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: urbanease-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "4000:4000"